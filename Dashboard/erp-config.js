/* ════════════════════════════════════════════════════════════════
   BALAJI NEXTGEN ERP — SHARED CONFIG
   ------------------------------------------------------------------
   Referenced by the Inventory and Sales modules but was missing from
   the upload (harmless 404 — nothing currently reads a global from
   it), so this is a safe stub that also gives you one place to keep
   settings common to all three modules going forward, instead of
   duplicating them inside each HTML file.
   ════════════════════════════════════════════════════════════════ */
window.ERP_CONFIG = {
  companyName: 'Balaji Enterprises',
  version: '2.0'
};
