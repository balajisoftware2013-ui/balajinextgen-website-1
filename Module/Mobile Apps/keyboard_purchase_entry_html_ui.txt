<!-- ════════════════════════════════════════════════════════════════
     KEYBOARD-DRIVEN PURCHASE ENTRY
     Full keyboard control - Type, Arrow Keys, Enter, Tab
     ════════════════════════════════════════════════════════════════ -->

<div id="keyboardPurchaseSheet" class="sheet" style="display:none;">
  <div class="sheet-header">
    <div>
      <h2 class="sheet-title">⌨️ Keyboard Purchase Entry</h2>
      <div style="font-size:11px;color:var(--sub);margin-top:2px;">
        Pure keyboard control - Type, ↑↓ to navigate, Enter to select, Tab to next field
      </div>
    </div>
    <button class="sheet-close" onclick="closeAllSheets()">✕</button>
  </div>

  <div class="sheet-body" style="max-height:90vh;overflow-y:auto;padding:16px;">
    
    <!-- INVOICE HEADER -->
    <div style="background:var(--body-bg);padding:12px;border-radius:8px;margin-bottom:16px;">
      
      <!-- SUPPLIER SEARCH (Keyboard) -->
      <div style="margin-bottom:12px;">
        <label style="font-size:11px;color:var(--sub);font-weight:600;display:block;margin-bottom:6px;">
          Supplier <span style="color:var(--red);">*</span>
        </label>
        <div id="supplierSearchContainer" style="position:relative;"></div>
      </div>

      <!-- INVOICE NO & DATE -->
      <div style="display:flex;gap:12px;flex-wrap:wrap;">
        
        <div style="flex:1;min-width:150px;">
          <label style="font-size:11px;color:var(--sub);font-weight:600;display:block;margin-bottom:4px;">
            Invoice No <span style="color:var(--red);">*</span>
          </label>
          <input type="text" 
            id="invoiceNo" 
            placeholder="INV-001"
            class="kbd-line-input"
            style="width:100%;padding:8px;font-size:12px;">
        </div>

        <div style="flex:1;min-width:150px;">
          <label style="font-size:11px;color:var(--sub);font-weight:600;display:block;margin-bottom:4px;">
            Date <span style="color:var(--red);">*</span>
          </label>
          <input type="date" 
            id="invDate" 
            class="kbd-line-input"
            style="width:100%;padding:8px;font-size:12px;">
        </div>

      </div>

      <!-- HIDDEN FIELD FOR SUPPLIER ID -->
      <input type="hidden" id="supplierSelect">
      <div id="supplierName" style="margin-top:8px;font-size:12px;font-weight:600;color:var(--primary);"></div>
      
    </div>

    <!-- LINE ITEMS HEADER -->
    <div style="padding:8px;background:#333;color:#fff;border-radius:8px 8px 0 0;display:flex;gap:6px;font-size:11px;font-weight:600;">
      <div style="flex:2;min-width:200px;">Item Name</div>
      <div style="flex:0.6;min-width:60px;">Qty</div>
      <div style="flex:0.5;min-width:50px;text-align:center;">Unit</div>
      <div style="flex:0.7;min-width:70px;">Rate</div>
      <div style="flex:0.5;min-width:50px;text-align:center;">GST %</div>
      <div style="flex:0.9;min-width:80px;text-align:right;">Amount</div>
      <div style="flex:0.3;min-width:35px;"></div>
    </div>

    <!-- LINE ITEMS TABLE -->
    <div id="lineItemsTable" style="border:1px solid var(--border);border-top:none;border-radius:0 0 8px 8px;background:#fff;margin-bottom:16px;">
      <!-- Items added here -->
    </div>

    <!-- TOTAL ROW -->
    <div style="background:var(--body-bg);padding:12px;display:flex;justify-content:flex-end;gap:20px;font-weight:700;border-radius:0 0 8px 8px;margin-bottom:16px;">
      <span>Total:</span>
      <span id="totalAmount" class="mono" style="min-width:100px;text-align:right;">₹0</span>
    </div>

    <!-- ACTION BUTTONS -->
    <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:16px;">
      <button class="btn btn-primary" style="flex:1;min-width:150px;" onclick="addKeyboardLineItem()">
        ➕ Add Item (Ctrl+N)
      </button>
      <button class="btn btn-primary" style="flex:1;min-width:150px;" onclick="saveKeyboardPurchase()">
        💾 Save Invoice (Ctrl+S)
      </button>
    </div>

    <!-- HELP -->
    <div style="background:#e8f5e9;padding:12px;border-left:4px solid var(--green);border-radius:4px;">
      <div style="font-weight:600;color:var(--green);margin-bottom:6px;">⌨️ Keyboard Navigation:</div>
      <div style="font-size:12px;color:#2e7d32;line-height:1.8;">
        • <b>Type</b> to search items/suppliers<br>
        • <b>↑↓</b> arrow keys to navigate results<br>
        • <b>Enter</b> to select from dropdown<br>
        • <b>Tab</b> to move to next field<br>
        • <b>Enter</b> (from last field) to add new line<br>
        • <b>Escape</b> to close dropdown<br>
        • <b>Ctrl+N</b> to add item<br>
        • <b>Ctrl+S</b> to save invoice
      </div>
    </div>

  </div>
</div>

<!-- KEYBOARD SHORTCUTS -->
<script>
document.addEventListener('DOMContentLoaded', function(){
  document.addEventListener('keydown', function(e){
    // Ctrl+N = Add Item
    if(e.ctrlKey && e.key === 'n'){
      e.preventDefault();
      const sheet = document.getElementById('keyboardPurchaseSheet');
      if(sheet && sheet.style.display !== 'none'){
        addKeyboardLineItem();
      }
    }
    
    // Ctrl+S = Save
    if(e.ctrlKey && e.key === 's'){
      e.preventDefault();
      const sheet = document.getElementById('keyboardPurchaseSheet');
      if(sheet && sheet.style.display !== 'none'){
        saveKeyboardPurchase();
      }
    }
  });
});

// Auto-fill date
setTimeout(() => {
  const dateInput = document.getElementById('invDate');
  if(dateInput && !dateInput.value){
    dateInput.value = new Date().toISOString().slice(0,10);
  }
}, 500);
</script>
