<!-- ════════════════════════════════════════════════════════════════
     INVOICE IMPORT SHEET - Insert into Business OS HTML
     Add this after other sheet definitions (search for: <div id="docSheet">)
     ════════════════════════════════════════════════════════════════ -->

<!-- IMPORT PURCHASES FROM EXCEL -->
<div id="invoiceImportSheet" class="sheet" style="display:none;">
  <div class="sheet-header">
    <h2 class="sheet-title">📥 Import Purchase Invoices</h2>
    <button class="sheet-close" onclick="closeAllSheets()">✕</button>
  </div>

  <div class="sheet-body" style="display:flex;flex-direction:column;gap:12px;">
    
    <!-- TABS -->
    <div style="display:flex;gap:6px;border-bottom:2px solid var(--border);margin-bottom:12px;">
      <button class="tab-btn active" onclick="switchImportTab('excel',this)" style="padding:10px 16px;border:none;background:none;cursor:pointer;border-bottom:3px solid var(--primary);font-weight:600;color:var(--primary);">📊 Excel Bulk Import</button>
      <button class="tab-btn" onclick="switchImportTab('ai',this)" style="padding:10px 16px;border:none;background:none;cursor:pointer;border-bottom:3px solid transparent;font-weight:600;color:var(--sub);">🤖 AI Invoice Scan</button>
    </div>

    <!-- TAB 1: EXCEL IMPORT -->
    <div id="tab-excel" style="display:block;">
      <div style="background:var(--primary-light);padding:12px;border-radius:8px;margin-bottom:12px;">
        <div style="font-weight:600;color:var(--primary);margin-bottom:6px;">📋 How to use:</div>
        <div style="font-size:12px;color:var(--text);line-height:1.6;">
          1. <b>Download</b> the template Excel file<br>
          2. <b>Fill in</b> your purchase data (follow instructions)<br>
          3. <b>Save</b> the Excel file<br>
          4. <b>Upload</b> here to import all invoices
        </div>
      </div>

      <div style="display:flex;gap:8px;margin-bottom:16px;flex-wrap:wrap;">
        <button class="btn btn-primary" onclick="downloadPurchaseTemplate()" style="flex:1;">⬇️ Download Template</button>
      </div>

      <div style="border:2px dashed var(--border);border-radius:8px;padding:20px;text-align:center;background:var(--body-bg);cursor:pointer;" onclick="document.getElementById('purchaseImportFile').click();">
        <div style="font-size:28px;margin-bottom:8px;">📁</div>
        <div style="font-weight:600;margin-bottom:4px;">Click to upload Excel file</div>
        <div style="font-size:12px;color:var(--sub);">or drag & drop</div>
        <input type="file" id="purchaseImportFile" accept=".xlsx,.xls" style="display:none;" onchange="handlePurchaseImportFile(this)">
      </div>

      <div id="importErrors" style="margin-top:12px;"></div>
      <div id="importPreview" style="margin-top:12px;max-height:400px;overflow-y:auto;"></div>
      <div style="margin-top:12px;font-size:12px;color:var(--sub);" id="importCount"></div>

      <button id="importConfirmBtn" class="btn btn-primary" style="margin-top:16px;width:100%;display:none;" onclick="confirmImportPurchases()">✅ Import All Invoices</button>
    </div>

    <!-- TAB 2: AI INVOICE SCAN -->
    <div id="tab-ai" style="display:none;">
      <div style="background:#e6f7ff;padding:12px;border-radius:8px;margin-bottom:12px;border-left:4px solid #0066cc;">
        <div style="font-weight:600;color:#0066cc;margin-bottom:6px;">🤖 AI-Powered Invoice Scanning</div>
        <div style="font-size:12px;color:#003366;line-height:1.6;">
          Upload an invoice photo or PDF. AI will extract:<br>
          ✓ Invoice number & date<br>
          ✓ Supplier name<br>
          ✓ Items, quantities, rates<br>
          ✓ GST details<br>
          Then review and save!
        </div>
      </div>

      <div style="border:2px dashed var(--border);border-radius:8px;padding:20px;text-align:center;background:var(--body-bg);cursor:pointer;" onclick="openInvoiceScanAI();">
        <div style="font-size:28px;margin-bottom:8px;">📸</div>
        <div style="font-weight:600;margin-bottom:4px;">Upload Invoice Image or PDF</div>
        <div style="font-size:12px;color:var(--sub);">JPG, PNG, or PDF - AI will read it</div>
        <input type="file" id="invoiceScanFile" accept=".pdf,.jpg,.jpeg,.png" style="display:none;" onchange="handleInvoiceScanFile(this)">
      </div>

      <div id="invoiceScanStatus" style="margin-top:12px;"></div>

      <!-- AI Extracted Form -->
      <div id="aiExtractedForm" style="margin-top:16px;display:none;">
        <div style="background:var(--body-bg);padding:12px;border-radius:8px;">
          
          <div style="display:flex;gap:12px;margin-bottom:12px;flex-wrap:wrap;">
            <div style="flex:1;min-width:150px;">
              <label style="font-size:12px;color:var(--sub);font-weight:600;display:block;margin-bottom:4px;">Invoice No</label>
              <input type="text" id="aiInvNo" style="width:100%;padding:8px;border:1px solid var(--border);border-radius:4px;">
            </div>
            <div style="flex:1;min-width:150px;">
              <label style="font-size:12px;color:var(--sub);font-weight:600;display:block;margin-bottom:4px;">Date (YYYY-MM-DD)</label>
              <input type="date" id="aiDate" style="width:100%;padding:8px;border:1px solid var(--border);border-radius:4px;">
            </div>
            <div style="flex:2;min-width:200px;">
              <label style="font-size:12px;color:var(--sub);font-weight:600;display:block;margin-bottom:4px;">Supplier Name</label>
              <input type="text" id="aiSupplier" placeholder="Must exist in Suppliers list" style="width:100%;padding:8px;border:1px solid var(--border);border-radius:4px;">
            </div>
          </div>

          <div style="font-weight:600;margin-bottom:8px;margin-top:16px;">Items (edit if needed):</div>
          <div id="aiItemsList"></div>

          <div style="display:flex;gap:12px;margin-top:12px;flex-wrap:wrap;">
            <div style="flex:1;min-width:200px;">
              <label style="font-size:12px;color:var(--sub);font-weight:600;display:block;margin-bottom:4px;">Remarks</label>
              <textarea id="aiRemarks" style="width:100%;padding:8px;border:1px solid var(--border);border-radius:4px;height:60px;resize:none;"></textarea>
            </div>
            <div style="text-align:right;padding-top:20px;font-size:14px;">
              <div style="color:var(--sub);font-size:12px;margin-bottom:4px;">Total Amount:</div>
              <div style="font-size:18px;font-weight:700;color:var(--primary);" id="aiTotal">₹0</div>
            </div>
          </div>

          <button class="btn btn-primary" style="width:100%;margin-top:16px;" onclick="saveInvoiceFromAI()">💾 Save This Invoice</button>
        </div>
      </div>

    </div>

  </div>
</div>

<!-- CSS STYLES - Add to <style> section -->
<style>
.tab-btn {
  transition: all 0.3s ease;
}
.tab-btn:hover {
  background: var(--body-bg);
}

#aiExtractedForm {
  animation: slideDown 0.3s ease;
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
</style>

<!-- JAVASCRIPT INTEGRATION - Add before closing </body> -->
<script>
function switchImportTab(tab, el){
  // Hide all tabs
  document.querySelectorAll('[id^="tab-"]').forEach(t => t.style.display = 'none');
  // Show selected tab
  document.getElementById(`tab-${tab}`).style.display = 'block';
  
  // Update button styles
  document.querySelectorAll('.tab-btn').forEach(b => {
    b.style.color = 'var(--sub)';
    b.style.borderBottomColor = 'transparent';
  });
  el.style.color = 'var(--primary)';
  el.style.borderBottomColor = 'var(--primary)';
}

// Show form when AI scans
document.addEventListener('DOMContentLoaded', function(){
  // Hook into parseAndPreviewImport to show form
  const originalPopulate = window.populateInvoiceFormFromAI;
  if(originalPopulate){
    window.populateInvoiceFormFromAI = function(data){
      document.getElementById('aiExtractedForm').style.display = 'block';
      originalPopulate(data);
    };
  }
});
</script>
